# PostgreSQL Database
DATABASE_URL=postgres://user:password@host:5432/database?sslmode=require
PGSSLMODE=require

# Redis Cache
REDIS_URL=redis://default:password@host:6379/0

# JWT Authentication
JWT_SECRET=generate-a-64-character-random-hex-string

# Email Configuration (Optional - leave blank to log preview links)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=587
SMTP_USER=verification@godlyme.com
SMTP_PASS=your-email-password
EMAIL_FROM="Godly Me Verification <verification@godlyme.com>"

# Application Settings (Optional)
APP_BASE_URL=https://your-app.vercel.app
UPLOAD_MAX_BYTES=209715200
PORT=4000
